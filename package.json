{
  "name": "aws-sso-credentials",
  "version": "0.0.1",
  "description": "Generate AWS credentials from AWS SSO",
  "bin": {
    "aws-sso-credentials": "./bin/aws-sso-credentials.js"
  },
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc --module commonjs --target es5 --outDir dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/ini": "^1.3.31",
    "@types/node": "^20.5.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "ini": "^4.1.1"
  }
}
